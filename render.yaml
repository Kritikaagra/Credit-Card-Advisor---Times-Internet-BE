services:
  - type: web
    name: credit-card-advisor-backend
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python data/init_db.py
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port 8000